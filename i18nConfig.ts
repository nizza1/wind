/**
 * ? this config  file will change the routes based on the locales
 * * the base URL followed by //en , de 
 *  */

const i18nConfig = {
    locales: ['en', 'de', 'it'],
    defaultLocale: 'en'
} as const;

export default i18nConfig;




