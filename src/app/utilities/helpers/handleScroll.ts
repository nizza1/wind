"use client"

export const handleScroll = function (href: string): void {
    console.log(href);

    /* if (href) {
        const targetId = href.replace(/.*\#/, "");
  
        // Get the element by id and use scrollIntoView with smooth behavior
        const elem = document.getElementById(targetId);
        elem?.scrollIntoView({
          behavior: "smooth",
        });
      } */
}


export const Handlee = (text: string = 'notext'): void => {
    console.log(text);
}
